<p id="notice"><%= notice %></p>

<h1><%= @show.name %></h1>

<p><%= link_to 'Episodes', show_episodes_path(@show) %></p>

<%= link_to 'Edit', edit_show_path(@show) %> |
<%= link_to 'Back', shows_path %>
